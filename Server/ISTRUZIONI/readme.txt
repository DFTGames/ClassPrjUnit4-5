1)Su smartfoxServer andare su Extension Manager e creare una cartella chiamata sfere,dentro questa cartella importare il file jar che si trova dentro la cartella dist contenuta SfereNetExtension

2)per la zona e la lobby vedere screen. guardare in particolare la voce Overridden user maximum idle time mettere un numero alto s eno i client si disconnettono troppo velocemente.